{
  "name": "simple-link-aggregator",
  "version": "1.0.0",
  "description": "A simple link aggregator built on AppScript",
  "main": "index.js",
  "scripts": {
    "watch": "nodemon --watch src --watch ../src --exec 'npm run build' -e ts,css,js",
    "build": "cd ../ && ./build.py && cd example && ts-node src/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.2.5"
  },
  "dependencies": {
    "@types/express": "^4.17.21",
    "express": "^4.19.0"
  }
}
